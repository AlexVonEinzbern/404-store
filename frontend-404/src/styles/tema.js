import { createTheme } from '@material-ui/core/styles';


export const tema = createTheme({
  palette: {
    primary: {
      main: '#fff',
    },
    secondary: {
      main: '#ab003c',
    },

    warning: {
        main: '#000',
      },
    
  },
});